var splide = new Splide('.splide-depositions', {
    perMove: 3,
    autoplay: false,
    interval: 4000,
    arrows: true,
  } );

splide.mount();

var splide = new Splide('.splide-detailed-depositions', {
  perMove: 1,
  autoplay: false,
  interval: 4000,
  arrows: true,
} );

splide.mount();